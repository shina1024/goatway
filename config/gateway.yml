schema_version: 1

proxy:
  # max_response_body_size_bytes: 10485760

throttle:
  # fail_policy: fail_open

circuit_breaker:
  # enabled: false
  # failure_threshold: 5
  # open_interval_ms: 30000
  # half_open_max_requests: 1

# trusted_proxies:
#   - 10.0.0.0/8
