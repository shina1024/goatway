- from:
    path: '^/sample/(.+)$'
    clients:
      - SampleClient
    ip_range_groups:
      - SampleIPRange
  to:
    destinations:
      - target_group: TargetGroupA
        path: '/$1'
      - target_group: TargetGroupB
        path: '/$1'

# Canary example (replace the active route above to try this split):
# - from:
#     path: '^/sample/(.+)$'
#     clients:
#       - SampleClient
#     ip_range_groups:
#       - SampleIPRange
#   to:
#     destinations:
#       - target_group: TargetGroupA
#         path: '/$1'
#         weight: 90
#       - target_group: TargetGroupB
#         path: '/$1'
#         weight: 10
